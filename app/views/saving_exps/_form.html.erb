<%= form_with(model: saving_exp, local: true) do |form| %>
  <% if saving_exp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saving_exp.errors.count, "error") %> prohibited this saving_exp from being saved:</h2>

      <ul>
      <% saving_exp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
